console.log('ciao')

// Il software deve chiedere per 10 volte all’utente di inserire un numero.
// Il programma stampa la somma di tutti i numeri inseriti.





// creo un array vuoto
const numeri = [];

// uso ciclo for per chiedere per 10 volte all'utente di inserire un numero parseInt((prompt)):
for(let i = 0; i < 10; i++){
    const numero = parseInt(prompt('inserisci un numero'));
    
    numeri.push(numero)
    console.log(numeri)
    // divido i numeri per sommarli tra loro:
        const numeriSplit = parseInt(numeri.split)
        console.log(numeriSplit)

    // sommo i numeri all'interno dell'array:
    
    // const somma = numero+numero;
    // console.log(somma)
}